# TODO

現時点でUIモック（画面構成・操作感）は一通り完成しています。
以降はゲームロジックの実装を優先度順に進めます。

## 1. 戦略カードの実装（優先度順）

現在「発掘」以外は `準備中`（未実装）です。実装しやすい順に並べています。

- [ ] 鑑定（コスト1）：他プレイヤーの宝石カード1枚を見る（状態を変更しない、閲覧のみ）。
      `HandCard.revealedToSelf` フィールドと、プレイヤー表示エリアのタップで手札表示エリアの
      対象を切り替える仕組みは実装済み。あとは「鑑定」実行時に対象カードの
      `revealedToSelf` を `true` にするロジックを`GameController`に追加すればよい
- [ ] 調査（コスト1）：任意の山札のすべてのカードを見る（閲覧のみ）
- [ ] 独占（コスト1）：任意の山札を効果の対象外にする（`Deck.monopolizedBy` は実装済み、UIから設定する導線がまだ無い）
- [ ] 保護（コスト1）：手札を任意の枚数、効果対象外にする（`HandCard.protected` フィールドは追加済み、実際に付け外しするロジックがまだ無い）
- [ ] 埋葬（コスト1）：手札のカード1枚を山札の1番上に置く
- [ ] 整地（コスト1）：任意の山札2つを1〜5枚ずつに作り変える
- [ ] 強奪（コスト2）：他プレイヤーの宝石を自分の手札にする
- [ ] 捏造（コスト2）：手札と山札のカードを交換する
- [ ] 取引（コスト2）：他プレイヤーとカードを1枚ずつ交換する

閲覧系（鑑定・調査）は状態を変更しないため実装・検証が容易で、最初の着手先として推奨。

## 2. DAI（山札の中身）の非公開情報化

現在は簡略化のため、山札の中身（DAI）が全プレイヤーに見える状態。
本来は非公開情報として扱うべきかどうかを先に決めてから、以下を進める。

- [ ] 非公開にする場合：山札カードの色ヒント表示を削除し、「調査」戦略で見た時だけ
      一時的に開示する仕組みに変更する
- [ ] 現状維持する場合：README・画面文言に「簡略化のため公開」である旨を明記する

## 3. 見た目の仕上げ

- [ ] 宝石の正式イラスト・アセットを用意し、`GemIcon`（CustomPaintの仮実装）を差し替える
- [ ] 戦略カードの説明文が長い場合の省略表示・詳細ダイアログの検討

## 4. ゲーム終了・得点まわりの検証

- [ ] `scoreHand` のボーナス条件（5色ボーナス・同色5枚ボーナス）を各戦略カード実装後に再検証
- [ ] 手札上限到達以外の終了条件（山札枯渇時のスコア計算など）のテスト拡充

## 5. オンライン対戦・永続化（後回し）

- [ ] Firebase等を用いたオンライン対戦（README記載の通り、当面は対象外）
- [ ] ゲーム状態の保存・再開
